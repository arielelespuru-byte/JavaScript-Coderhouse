let nombreUsuario = prompt("Ingrese su nombre de usuario: ");
let importe = Number(prompt("Ingrese el importe en $: "));
let cuotas = parseInt(prompt("ingrese la cantidad de cuotas:  "));
const IVA = 21
console.log(nombreUsuario);
console.log(importe);
console.log(cuotas);

let resultado = (importe * 1.20) / cuotas; 
alert("Su prestamo queda en: " + resultado.toFixed(2) + " $" + " por mes");
